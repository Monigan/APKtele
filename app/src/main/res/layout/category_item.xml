<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="11dp">


    <ImageView
        android:id="@+id/applicationIco"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:contentDescription="@string/categories"
        app:layout_constraintBottom_toTopOf="@+id/applicationTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_cat_bg" />

    <TextView
        android:id="@+id/applicationTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="@font/inter"
        android:text="@string/categories"
        android:textColor="@color/main_text_color"

        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@+id/applicationIco"
        app:layout_constraintStart_toStartOf="@+id/applicationIco"
        app:layout_constraintTop_toBottomOf="@+id/applicationIco" />
</androidx.constraintlayout.widget.ConstraintLayout>